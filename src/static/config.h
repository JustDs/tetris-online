#ifndef STATIC_CONFIG_H
#define STATIC_CONFIG_H
enum
{
	//window size
	WINDOW_WIDTH = 800,
	WINDOW_HEIGHT = 600,
	
	//view settings
	MAX_FRAMES = 60,

	//timer interval
	TIME_INTERVAL = 500
};
#endif

